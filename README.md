# Online-Bookstore
A PHP-based Bookstore web application running on a XAMPP server with a MySQL database created via phpMyAdmin. It includes user authentication (login, register, logout, checklogin), book browsing and checkout features, user profile editing, and is styled with CSS for a clean, responsive interface.
